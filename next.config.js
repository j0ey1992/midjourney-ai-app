/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "images.unsplash.com",
      "images.pexels.com",
      "cdn.discordapp.com",
    ],
  },
}

module.exports = nextConfig
